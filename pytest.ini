[pytest]
minversion = 7.0
addopts =
    -ra
    --strict-markers
    --disable-warnings
markers =
    unit: fast, isolated tests (no I/O, no network)
    functional: end-to-end/behavioral tests; may touch disk or CLI
testpaths =
    tests
